# 🚀 QUICKSTART — Apostilas EBE no Telemóvel (Termux + ACode)

## 1. Instalar Termux + ACode
- Baixa **Termux** (F-Droid recomendado)
- Baixa **ACode** (editor de código excelente para Android)

## 2. Configurar Termux (copia e cola)
```bash
pkg update && pkg upgrade -y
pkg install git python nodejs -y
pip install --upgrade pip
termux-setup-storage